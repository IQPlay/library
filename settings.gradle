// 📌 Le bloc `pluginManagement` doit être tout en haut
pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

// 📌 Ajout du plugin `foojay-resolver-convention` ici (et non dans `build.gradle`)
plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

// 📌 Le bloc `dependencyResolutionManagement` suit immédiatement `pluginManagement`
dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

// 📌 Définition du projet après `pluginManagement`
rootProject.name = 'IQPlayLibrary'
include('lib')
